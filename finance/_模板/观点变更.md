# <公司名>（<代码>）观点变更史

> **只增不改**：每次「改主意」在最上面加一条，旧条目永不删改。
>
> 与 `分析.md` 的分工：
> - `分析.md` = **当前怎么看**（财报来了会被 `caibao-analysis` 重刷，旧视图被覆盖）
> - 本文件 = **看法怎么变的、为什么**（不可变历史，刷新 `分析.md` 时不要动这里）
>
> 记录时机：评级变 / 买卖点重估 / 论点（护城河·三前提）改写 / 最高仓位调整 / 归入或移出「无法估值(太难)」或「回避(逻辑变坏)」。
>
> 格式：`YYYY-MM-DD 变更类型:前→后 | 为什么（数据依据）`
> 变更类型：首次建库 · 调买卖点 · 改评级 · 改论点 · 调仓位 · 归无法估值(太难) · 归回避(逻辑变坏) · 重新跟踪

---

- YYYY-MM-DD 首次建库 | <初始评级 + 买点 + 一句话依据>
